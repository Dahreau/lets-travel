{
  "name": "lets-travel-e2e",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "playwright test",
    "postinstall": "playwright install chromium --with-deps"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
